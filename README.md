# Ansible Tools


## Setup requirements

```bash
make setup
```

## Playbooks
Create DNS record via Cloudflare

```bash
make dns server_name=dev.rocknblock.io host=dev3
```

Deploy NGINX config

```bash
make dns server_name=dev.rocknblock.io host=dev3
```
